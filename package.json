{
  "private": true,
  "devDependencies": {
    "lerna": "2.0.0-beta.15",
    "mocha": "^2.5.3",
    "pem": "^1.8.3",
    "standard": "^7.1.1"
  },
  "scripts": {
    "lint": "standard",
    "unit": "mocha --recursive packages/*/test/ -t 5000",
    "bootstrap": "lerna bootstrap",
    "test": "npm run unit && npm run lint"
  }
}
